#!/bin/bash

echo "# Random texts with different sigma, base length, repeats. Generated by bin/random"
echo "# Added to test_case.config by add_rnd_files_to_config.sh"
for s in 4 16 64; do
    for l in 100000 1000000 10000000; do
        for r in 1 2 4; do
            echo "RND-${s}-${l}-${r};data/rnd.${s}.${l}.${r};rnd.${s}.${l}.${r};"
        done
    done
done
